# Experiment results

The evidence. This file is the index; each run has its own report under
`results/`. Both are generated, so do not edit either by hand.

**No run has been scored yet.**

The stage 4 gate is closed until at least one run appears here. No encoder
parameter belongs in `packages/encoder` and no copy may assert a quality
gain. See `experiments/README.md` for the protocol.
